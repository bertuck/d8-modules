<?php

/**
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 */
function kb_error_pages_theme($existing, $type, $theme, $path) {
    return [
        'access-denied' => [
            'variables' => ['error' => NULL],
        ],
        'not-found' => [
            'variables' => ['error' => NULL],
        ]
    ];
}
